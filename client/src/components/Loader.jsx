import React from 'react';
import '../Loader.css'; // Assuming the CSS is in a separate file

const Loader = () => {
  return (
    <div className='loader-container'>
      <div className='loader'></div>
    </div>
  );
};

export default Loader;
